---
title: Logarithmic Equations
tag: MTD
track: school
grade: 11
subject: algebra
requires: [logarithmic-function, logarithm-properties]
---

Monotonicity again turns $\log_a f = \log_a g$ into $f = g$, but the transformations of logarithms may widen the domain, so the conditions $f > 0$, $g > 0$ belong to the solution, not to the check.
